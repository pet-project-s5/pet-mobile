# ─── API local (fora do Docker) ─────────────────────────────────────────────────
# Copie para .env e preencha.
# Em Docker: as vars vêm do docker-compose.yml — este arquivo não é usado.

DATABASE_URL="postgresql://postgres:troque_a_senha@localhost:5432/petapi"
JWT_SECRET="troque_por_uma_chave_longa_e_aleatoria"
PORT=8080
