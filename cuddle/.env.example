# ─── Front-end Expo (variáveis de build) ───────────────────────────────────────
# Copie para .env e preencha. Nunca commitar o .env real.
#
# Em Docker: a chave é passada via build arg no docker-compose.yml
# Em dev local (npx expo start): crie este arquivo como .env

EXPO_PUBLIC_GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxx
